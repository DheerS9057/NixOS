{
    "label" : "Logout",
    "action" : " xargs kill; pkill Hyprland || pkill sway || pkill niri || loginctl terminate-user $USER",
    "text" : "Logout",
    "keybind" : "e"
}
{
    "label" : "Shutdown",
    "action" : " xargs kill; systemctl poweroff || loginctl poweroff",
    "text" : "Shutdown",
    "keybind" : "s"
}
{
    "label" : "Reboot",
    "action" : "xargs kill; systemctl reboot || loginctl reboot",
    "text" : "Restart",
    "keybind" : "r"
}
